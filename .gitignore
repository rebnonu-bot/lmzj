# 忽略所有文件
*

# 允许忽略 README.md
!README.md

# 允许忽略 .gitignore 本身 (建议保留，以便同步忽略规则)
!.gitignore

# 允许忽略 dist 目录及其内容
!dist/
!dist/**

# 允许忽略 package.json (通常部署也需要，但如果用户明确只要 dist 和 readme，可以根据需要注释掉)
#!package.json
