import {
  coalesce,
  getInstance
} from "./chunk-7VUFE4BV.js";

// node_modules/@tdesign/uniapp/dist/toast/index.js
function Toast(options) {
  const { context, selector = "#t-toast", ...Options } = options;
  const instance = getInstance(context, selector);
  if (instance) {
    instance.show({
      ...Options,
      duration: coalesce(Options.duration, 2e3)
    });
  }
}
function showToast(options = {}) {
  Toast(options);
}
function hideToast(options = {}) {
  const { context, selector = "#t-toast" } = options;
  const instance = getInstance(context, selector);
  if (instance) {
    instance.hide();
  }
}
export {
  Toast as default,
  hideToast,
  showToast
};
//# sourceMappingURL=@tdesign_uniapp_toast_index__js.js.map
